import { newlineToBreak } from "./breaks.lil";

JsValue transform(JsValue _s, JsValue tree, JsValue _file) {
  newlineToBreak(tree);
  return JS.undefined();
}

JsValue attach(JsValue _self, JsValue _options) {
  return JS.method2(transform);
}

export JsValue remarkBreaks = JS.method1(attach);
export JsValue default = remarkBreaks;
